server:
  port: 8585
spring:
  application:
    name: configuration-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bignuttawuts/configuration-service.git
          default-label: main
          clone-on-start: true